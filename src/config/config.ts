export enum AccountTypeEnum {
  main = 'main',
  smart = 'smart',
}

export enum Network {
  pangolin = 'pangolin',
  crab = 'crab',
  darwinia = 'darwinia',
  main = 'darwinia',
}

export const DEFAULT_DECIMALS = 9;

// tslint:disable-next-line: no-magic-numbers
export const PRECISION = Math.pow(10, 9);
